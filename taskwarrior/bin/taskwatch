#!/usr/bin/env fish

echo "watching tasks..."
inotifywait -m -e modify ~/Sync/task | while read path event file;
    if test $file = "undo.data"
        echo "exporting tasks to ~/Sync/tasks.json ..."
        task export status:pending > ~/Sync/tasks.json
        echo "... exported tasks!"
    end
end

